---
description: Teach the new performance gate to check that a query is not drastically slower than its faster twin in the same run, with a re-measure step so a noisy machine cannot produce false failures.
prereq: bench-gate-reference
files:
  - packages/quereus/bench/lib/guards.mjs           # DONE — checkRatioGuards + report + remeasure/exit pure fns
  - packages/quereus/bench/run.mjs                  # DONE — moved-out functions deleted, imports lib/guards.mjs
  - packages/quereus/bench/lib/calibrate.mjs        # DONE — GATE_CALIBRATION added
  - packages/quereus/bench/lib/discover.mjs         # DONE — validateRatioGuards + RatioGuard typedef (note, cross-suite)
  - packages/quereus/bench/child.mjs                # DONE — --gate-calibration flag wired
  - packages/quereus/bench/gate.mjs                 # DONE (unvalidated) — guard pass, fork helper, --report-only
  - packages/quereus/test/bench-guards.spec.ts      # REMAINING — new test file
difficulty: hard
---

<!-- resume-note -->
TWO prior agent runs were interrupted by token-budget stops. Run 1 finished the
`bench/lib/` half (guards.mjs, run.mjs, discover.mjs, calibrate.mjs, child.mjs — all
verified consistent by run 2). Run 2 finished ALL of the gate.mjs work listed below,
but was stopped IMMEDIATELY after the last edit landed: **nothing from run 2 has been
validated** — no lint, no test run, no smoke. First step this run: `yarn workspace
@quereus/quereus lint` and read any resulting errors before writing the spec file.
IDE diagnostics at stop time showed no remaining errors in gate.mjs.

# Why (unchanged from original ticket)

`bench-gate-reference` (landed) gates on exact work counters. This ticket adds the second
portable measurement — a bound on one benchmark's median against another's *in the same
run* (a "ratio guard") — to `yarn bench:gate`. A within-run ratio cancels machine speed,
which is what makes it gateable when an absolute millisecond figure is not. A sibling
ticket `bench-gate-check-wiring` (prereq: this one) wires the gate into `yarn check`,
adds new guards, and updates docs. This ticket is the guard engine only.

# Already done (verify with lint, don't redo)

## bench/lib (run 1 — verified consistent by run 2)

- **`bench/lib/guards.mjs`** (NEW, complete, fully JSDoc-typed — in the tsconfig.test.json
  type pass). Exports: `resolveGuardName`, `guardMemberNames`, `checkRatioGuards`
  (status order: informational→misconfigured FIRST; skipped member→not-evaluated;
  unselected→skipped (filter) / misconfigured (no filter); ran-but-failed→not-evaluated;
  degenerate medians baseline 0 → target>0 ? Infinity : 1; ratio>maxRatio→failed else ok),
  `decideRemeasuredGuard`, `mergeRemeasuredVerdicts` (index-aligned, throws on length
  mismatch), `gateExitCode(failed, reportOnly)`, `reportRatioGuards(verdicts, {say, red,
  yellow})` → failed+misconfigured count, appends ` [note]`.
- **`bench/run.mjs`** — imports from `./lib/guards.mjs`; local copies deleted.
- **`bench/lib/discover.mjs`** — `validateRatioGuards` exported + called from `loadSuite`;
  `RatioGuard` typedef documents `note` and cross-suite `suite/name` resolution.
- **`bench/lib/calibrate.mjs`** — `GATE_CALIBRATION` = CALIBRATION spread with
  `warmupTargetMs: 100, targetTotalMs: 300, maxTotalMs: 1500`.
- **`bench/child.mjs`** — `--gate-calibration` selects GATE_CALIBRATION, passed to both
  `calibrate` and `collectSamples`.

## bench/gate.mjs (run 2 — COMPLETE but UNVALIDATED)

All architecture decisions from the original ticket were followed:

- Header comment rewritten (two passes), USAGE updated with `--report-only`.
- Imports added: fork/join/fileURLToPath, informationalNames/selectBenchmarks,
  guards.mjs functions, summarize, sweepBenchTempDirs.
- `parseArgs`: `--report-only` in BOOLEAN_FLAGS; `--report-only` + `--accept` →
  UsageError. `envReportOnly(value)` helper: on unless unset/''/'0'; env ignored in
  accept mode (folded in main: `args.reportOnly || (!accept && envReportOnly(...))`).
- New "timed guard pass" section: `forkGuardMember` mirrors run.mjs forkBenchmark
  (piped stdio → humanStream/stderr, IPC result/failure/skipped, 120 s SIGKILL +
  5 s reap backstop, settle on 'close'); args `[suiteFile, benchName, '--no-counters']`
  + `--gate-calibration` when reduced. `activeChild`/`killedWorkerPids` tracking,
  SIGINT/SIGTERM handlers kill active child + sweep, `sweepTempDirs()` wraps
  `sweepBenchTempDirs(killedWorkerPids)`.
- `timeGuardMembers(members, reducedCalibration, state)` — sequential forks; skipped →
  skippedNames map; failure → memberFailures push {fullName, detail}, red line, stderr
  tail, and **deletes** any stale allBenchmarks entry; success → `summarize(timings,
  {batch, warmup, pinned})` into allBenchmarks + progress line.
- `runGuardPass(suites, filter, informational)` — early-returns empty when no suite has
  guards; timed set = `selectBenchmarks(suites, filter)` ∩ `guardMemberNames(suites,
  informational)`; selectedNames = full selection; checkRatioGuards → failed verdicts →
  re-fork union of their members at FULL calibration (yellow announce line) → second
  checkRatioGuards → `mergeRemeasuredVerdicts`; sweeps temp dirs; prints guard-pass
  wall-clock over fork count.
- main(): guard pass runs in gate mode only, AFTER printGateReport. Exit folding:
  `failed = countersFailed || reportRatioGuards(...) > 0 || memberFailures.length > 0`.
  GATE FAILED block reworded per ticket (counters line + accept hint only when
  countersFailed; guard-failure and member-failure lines when they apply); green line is
  "...matches the reference and every ratio guard holds." REPORT-ONLY yellow box printed
  inside the failed branch when reportOnly; one-line yellow notice near top when active.
- Zero-selection relax: when `selectedCount === 0`, gate mode also counts
  `matchesFilter` over `guardMemberNames` and errors only when both are zero; accept
  keeps the strict rule (guardMembersSelected forced 0).
- JSON additions: `ratio_guards` (null in accept mode — not evaluated),
  `guard_member_failures` (null in accept mode), `report_only`; `failed` stays honest
  under report-only. Exit via `process.exitCode = gateExitCode(failed, reportOnly)`.
- Counters wall-clock line relabelled "Counters pass wall-clock: ..." since the gate now
  times things in pass two.

# Remaining work

## Validate run 2's gate.mjs edits (FIRST)

`yarn workspace @quereus/quereus lint` (type pass covers bench/lib + test; gate.mjs
itself is outside tsconfig.test.json include but eslint still runs over it). Fix
anything it reports.

## test/bench-guards.spec.ts (NEW — runs no benchmark)

Import from `../bench/lib/guards.mjs` and `validateRatioGuards` from
`../bench/lib/discover.mjs` (pattern: `test/bench-gate.spec.ts`; chai expect; fixtures
are cheap — `checkRatioGuards` only reads `suite.name`, `suite.ratioGuards`, and
`median_ms` off summaries, so `{ median_ms: 5 }` objects suffice). Cover:

- Every `checkRatioGuards` status: ok; failed; skipped under filter; misconfigured
  without one; not-evaluated for a member absent from allBenchmarks (ran-but-failed);
  not-evaluated for a skipped member; misconfigured for an informational member checked
  FIRST (make the member also skipped + unselected to prove precedence). Degenerate
  medians: baseline 0 & target>0 → failed with Infinity; both 0 → ok ratio 1.
- Name resolution: bare resolves in own suite; `suite/name` crosses suites; cross-suite
  name at a nonexistent benchmark → misconfigured (no filter). `note` carried into the
  verdict.
- `validateRatioGuards`: throws on missing name / missing baseline / maxRatio absent,
  zero, negative, non-finite, non-number / non-string note; accepts a valid entry with
  and without note; non-array export throws.
- `decideRemeasuredGuard` / `mergeRemeasuredVerdicts`: fail-then-pass → final ok,
  `remeasured: true`, carries `reducedRatio` and final `ratio`, detail names both;
  fail-then-fail stays failed; non-failed initial verdicts pass through untouched
  (identity); length mismatch throws.
- `gateExitCode`: (true,false)→1, (true,true)→0, (false,*)→0.
- `guardMemberNames`: collects both members, resolves cross-suite, drops members of an
  informational-poisoned guard, keeps a shared member reachable via a healthy guard.
- `reportRatioGuards`: with a collecting `say` and identity colours — returns
  failed+misconfigured count; prints the note.

TS tip from run 2's planning: get the verdict type as
`type GuardVerdict = ReturnType<typeof checkRatioGuards>[number]` so hand-built
fail/ok verdict literals type-check against the status union.

## Validation

`yarn workspace @quereus/quereus test` and `yarn workspace @quereus/quereus lint`.
A quick smoke: `node bench/gate.mjs --filter correlated-subquery` from packages/quereus
(build first if dist stale) — exercises fork, reduced calibration, verdict print, exit 0.
Optionally set the guard's maxRatio low in a scratch edit to see the re-measure path
fire, then revert.

## Handoff

Distilled summary into review/ — honest notes: GATE_CALIBRATION values (100/300/1500 ms)
are chosen, not measured — reviewer may want one timed gate run to sanity-check spread.
gate.mjs guard pass has NO unit coverage (it's process wiring; the logic is in
lib/guards.mjs which does) — smoke run is the only end-to-end exercise.

# Not in scope (unchanged)

Wiring into `yarn check`, adding new guards, measuring wall-clock, doc updates — that is
`bench-gate-check-wiring` (prereq: this ticket). Performance sentinels reconciliation
(`debt-perf-sentinels-share-bench-workloads`). Wall-clock-bounded join-order search
(`bug-join-order-depends-on-wall-clock`).

## TODO

- lint → fix any fallout from run 2's gate.mjs edits
- test/bench-guards.spec.ts per the list above
- `yarn workspace @quereus/quereus test` + `lint` green; smoke the gate once
- Hand off to review/ with the honest notes above
