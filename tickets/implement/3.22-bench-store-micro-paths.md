---
description: Add benchmarks that price the storage layer's remaining hot paths one at a time - scanning, point reads, fetching rows found through a secondary index, committing a batch of writes, building an index, and reopening a database - each reporting how many times it went to storage, not just how long it took.
prereq: bench-store-micro-keys
files:
  - packages/quereus/bench/suites/store.bench.mjs               # created by the prereq; this ticket appends to it
  - packages/quereus/bench/lib/store-counters.mjs               # openStoreDatabase / openCountingStoreDatabase / readStoreCounters
  - packages/quereus/bench/lib/counters.mjs                     # snapshotStatement / snapshotStatements
  - packages/quereus-store/src/common/store-table-scan.ts       # ROW_RESOLUTION_BATCH = 256, resolveRowBatch
  - packages/quereus-store/src/common/transaction.ts            # TransactionCoordinator - the commit path
  - packages/quereus-store/src/common/store-module-index-build.ts # buildIndexEntries
  - packages/quereus-store/src/common/store-module-catalog.ts    # catalog write / rehydrate
  - packages/quereus/bench/apply-schema-unchanged.mjs           # reuse its 54-table / 14-view declaration shape
  - docs/benchmarking.md
difficulty: hard
---

<!-- resume-note -->
## Progress notes (two prior runs — ALL investigation done and EMPIRICALLY VERIFIED, no code written yet)

Two prior agent runs spent their budgets on discovery. Run 2 wrote a throwaway probe
script (since deleted; tree is clean), drove every mechanism this ticket needs against
the built dist, and captured the actual counter numbers below. Nothing is unverified
any more — skip straight to writing the `bench/suites/store.bench.mjs` additions and the
one widening of `bench/lib/store-counters.mjs`. No partial edits exist anywhere.

**Probe-verified facts (run 2, exact numbers from a live run — design around these):**

Fixture used by the probe: `create table bench_t (id integer primary key, val integer)`,
secondary index on `val`, 10 000 rows with `val = id`, inserted 500 rows per statement.

- **Full scan** (`select id from bench_t`): data store `iterateEntries=10000`, all other
  counts zero everywhere. Plan picks the scan at 10k with no coaxing.
- **PK point read** (`where id = 5000`): data store `singleGets=1` (raw `getCount=1`),
  nothing else anywhere.
- **PK in-list multi-seek** (`where id in (…k literals…)`): data store
  `getManyCalls=ceil(k/ROW_RESOLUTION_BATCH)` carrying `getManyKeys=k` — measured k=10 → 1
  call, k=300 → 2 calls. Zero iterates on any store.
- **Secondary-index range** (`where val >= 2000 and val < 2000+n`): index store
  `iterateEntries=n`, data store `getManyCalls=ceil(n/256)` carrying `getManyKeys=n`, zero
  data-store iterates. Measured n=64→1, 256→1, 512→2, 1024→4. The planner picks the index
  at ALL these sizes against the 10k fixture, including n=1024 (~10% selectivity) — no
  plan-shape assertion needed beyond the counter assertions themselves.
- **Commit shape** (`begin; update bench_t set val = val where id < N; commit;` on the
  INDEXED fixture): per commit, **`batchWrites=1` on the data store and 1 on the index
  store — FLAT across N ∈ {1,10,100,1000}** (the per-store fallback: one `WriteBatch` per
  touched store). `batchOps=N` on data, `2N` on index (each touched row rewrites its index
  entry as delete+put). Reads provoked per commit: data `iterateEntries=N` (the PK-range
  scan finding the rows) and `getCount=2N` (one effective-row read per update arm + one
  flush probe per row). `__stats__` and `__catalog__` untouched by commits.
- **A same-value update does NOT short-circuit anywhere** — the repeat commit of
  `set val = val` over the same 100 rows produced byte-identical counters to the first.
  Verified in code too: the SQL update arm in `store-table.ts` calls `coordinator.put`
  unconditionally, and the isolation flush (`quereus-isolation/src/flush.ts`,
  `applyOverlayToUnderlying`) applies every live overlay row unconditionally. (Only the
  external-ops replication path `applyExternalRowOps` suppresses value-identical upserts —
  not reachable from SQL.) So a fixed `update … set val = val` workload is repeatable
  as-is; alternating two constants remains cheap insurance against a future echo-
  suppression seam, at author's discretion.
- **Index build create/drop cycle works on the plain provider**: 3 consecutive cycles of
  assert-empty → `create index` → count → `drop index` gave before=0 / after=5000 every
  cycle. Empty-before assertion shape:
  `const store = await provider.getIndexStore('main','bench_i','bench_i_val')` then count
  via `for await (const e of store.iterate())` — lazily creates an empty store when absent,
  which is exactly the "empty or absent" semantics the ticket asks to assert.
- **Catalog rehydration over the ISOLATED module works and is repeatable.** The wiring:
  build schema over `createIsolatedStoreModule({provider})` + fresh `Database` (the
  `attach()` shape in `bench/lib/store-counters.mjs`), one insert per table (catalog
  persist is LAZY — a table never touched persists no catalog entry), then
  `await module.underlying.whenCatalogPersisted()` — `IsolationModule.underlying` is a
  public readonly property holding the `StoreModule`. Close the fixture `Database` with
  `db.close()` ONLY — **never `module.closeAll()`, which calls `provider.closeAll()` and
  kills the shared provider** (verified in `store-module.ts` `closeAll`, last lines). Then
  per cycle: fresh `Database` + fresh `createIsolatedStoreModule({provider})` over the SAME
  provider, `await module2.underlying.rehydrateCatalog(db2)` → returns
  `{tables, indexes, views, materializedViews, errors}` (string[] + error list) — probe
  got tables=8/indexes=4/views=3/errors=0 twice in a row, and the reopened db queries the
  rehydrated tables fine. Views and secondary indexes DO rehydrate.
- **Rehydration counters**: after reset, rehydrating an 8-table catalog read
  `__catalog__ iterateEntries=8` (one entry per object, ONE scan), `getCount=1` (the
  clean-shutdown-marker probe), `__stats__ getCount=8` (one per table), and touched no
  data store. Expected block for the 54-table/14-view fixture: `__catalog__`
  iterateEntries = number of catalog objects (tables + indexes + views), one scan.
- The counters pass builder `openCountingStoreDatabase()` does NOT expose its provider or
  module — the rehydration and index-build counters passes need the widening below.

**Required widening of `bench/lib/store-counters.mjs`** (the sanctioned single import
site; keep it the only place touching `@quereus/store`):

- Let both builders accept an optional existing provider (mint one only when absent), and
  return the `module` (the `IsolationModule`) on the handle alongside the existing fields —
  that yields `module.underlying.rehydrateCatalog(db)` / `.whenCatalogPersisted()` and
  keeps `attach()` shared. The counting handle must also expose its `provider` so the
  rehydration counters pass can reopen over it. Preserve the existing contract:
  `readCounters()` before `close()`, and `close()` still closes module + db — callers that
  share a provider across cycles must close with `db.close()` only (add a `closeDbOnly`
  or equivalent on the handle, or document the pattern).

**Everything below this line from run 1 is still accurate; the two "UNVERIFIED" items at
its end are now settled by the probe results above (no short-circuit; erase confirmed).**

**Harness facts (all confirmed current):**

- `openStoreDatabase()` in `bench/lib/store-counters.mjs` already returns
  `{ db, close, provider }` — the provider-on-handle change the harness ticket promised has
  landed, so the index-build benchmark can reach physical stores.
- `openCountingStoreDatabase()` returns `{ db, close, resetCounters, readCounters }`;
  `readCounters()` throws after `close()` — read before closing. Counter block per store:
  `iterateEntries, getManyCalls, getManyKeys, singleGets, directPuts, directDeletes,
  batchWrites, batchOps`.
- `loadStoreKeyApi()` already exposes `ROW_RESOLUTION_BATCH` (shape-checked as a number) —
  import it from there; never restate 256.
- Counters-pass pattern to copy: `queryCounters` in `bench/suites/execution.bench.mjs`
  (fixture into counting db → `resetCounters()` → `snapshotStatement` → `readCounters()`
  → close in `finally`). `snapshotStatement` (bench/lib/counters.mjs) rejects
  multi-statement SQL; use `snapshotStatements` for sequences (e.g. `begin` / updates /
  `commit`).

**Things the new benchmarks must be written around:**

- `store.bench.mjs`'s file header (lines ~11–28) currently claims the suite has NO
  `counters()` pass, no `Database`, and no storage traffic. Appending these benchmarks
  falsifies that — revise the header (and `docs/benchmarking.md` § "The `store` suite",
  which repeats the claim, plus the "Which suites qualify" table row `store | 0 / 11`).
- Docs currently say 57 benchmarks total, store suite = 11 rows, store suite adds ~16 s,
  full run ~100 s. All these counts/timings need updating after the append (the ticket
  body's "~35 s" note is stale; so is its "46 benchmarks").
- `bench/apply-schema-unchanged.mjs` does NOT export `buildDeclaration` — it is a script
  that runs `main()` on import. Copy the declaration-generator *shape* (54 tables /
  14 views); do not import the file.
- The catalog-rehydration benchmark needs a database built over an EXISTING provider.
  `openStoreDatabase()` mints a fresh provider per call, so widen
  `bench/lib/store-counters.mjs` (the sanctioned single import site — its own header says
  widening is the intended move) with a builder that accepts a provider, e.g.
  `openStoreDatabase(existingProvider?)` reusing `attach()` +
  `createIsolatedStoreModule({ provider })`.
- `createCountingProvider` (packages/quereus-store/src/testing/kv-counting-store.ts) has a
  NOTE at ~line 193 confirming it implements neither `deleteIndexStore` nor
  `deleteTableStores` — the counters pass for the index build must create the index once
  and never drop it, exactly as the ticket says.
- `CountingKVStore.getMany` routes each key through its own counted `get` (so `getCount`
  includes batch keys — `singleGets` is derived in `readStoreCounters`). Write side:
  `batchWriteCalls`/`batchOpCount` count only per-store `WriteBatch` commits; `AtomicBatch`
  is deliberately uncounted and unreachable (counting provider exposes no
  `beginAtomicBatch`), so the commit benchmark's expected flat count is one `batchWrites`
  per touched store per commit (data store + one per touched index store + possibly
  `__stats__`).
- Commit path confirmed in `packages/quereus-store/src/common/transaction.ts` `commit()`:
  ops grouped by store handle; no atomic factory → one `store.batch()` + one `write()` per
  touched store, sequential. Explicit `begin;`/`commit;` SQL works.
- Reference counts confirmed against
  `packages/quereus-store/test/index-scan-batching.spec.ts`: a secondary-index read of n
  rows costs `ceil(n / ROW_RESOLUTION_BATCH)` `getManyCalls` on the DATA store carrying n
  keys, `iterateEntries` on the INDEX store, and zero data-store iterates. `where v in
  (...)` over an indexed column plans as multi-seek (plan=5) and batches ACROSS windows.
  A whole-PK `in (...)` multi-seek resolves via one bounded `getMany` too. A PK point read
  (`readLiveRowByPk`) lands in `singleGets`; a full scan is `iterateEntries` on the data
  store.
- Catalog rehydration reads via one `catalogStore.iterate(buildCatalogScanBounds())`
  (`loadCatalogEntries` in store-module-catalog.ts) — expect ONE scan's `iterateEntries` ≈
  entry count on `__catalog__`, not per-table gets. Reopen = new `Database` + new
  isolated module over the SAME provider instance.
- `buildIndexEntries` (store-module-index-build.ts) streams data entries and flushes the
  index batch by `maxBatchBytes` (DEFAULT_MAX_BATCH_BYTES) — so index-build `batchWrites`
  on the index store scale with total key bytes / budget, plus one final flush; assert a
  count, don't hand-derive the byte math in the benchmark, or assert entry count instead.

**Formerly UNVERIFIED — both now SETTLED by run 2's probe (kept for context only):**

1. Whether a same-value `update` short-circuits the write (ticket's commit-benchmark
   workload question). Not yet read: the store-table mutation layer (xUpdate path,
   `packages/quereus-store/src/common/store-table*.ts`) AND the isolation wrapper above it
   — either layer could drop a no-op write. If unsure after reading, just alternate the
   written value between two constants; that sidesteps the question entirely and is the
   safer default.
2. That `createInMemoryProvider` (packages/quereus-store/src/testing/, exact file not yet
   read) implements `deleteIndexStore` as an erase (ticket asserts it does). The
   before-each-`fn` "index store empty or absent" assertion covers the risk either way.

# Why

These are the paths that run per row, per index, per commit — the ones where a per-operation
cost that should have been per-batch can hide behind a whole-query number.

Unlike the key-encoding benchmarks the prereq adds, none of these can be reached without
white-boxing the store. So they are driven through a `Database` and **reported in storage
round trips as well as time**, which is the honest measurement anyway: round-trip counts are
exact integers, identical on every machine, where a wall-clock median describes one CPU.

# Architecture

Append to `bench/suites/store.bench.mjs`, which the prereq creates. Same rules apply: bare
names with no backend suffix, `skip()` wired to `storeLoadFailure()`, fixtures built in
`setup`, every `fn` repeatable back-to-back and asserting its own result.

The timed database comes from `openStoreDatabase()` and the untimed `counters()` pass from
`openCountingStoreDatabase()`, both in `bench/lib/store-counters.mjs` — a second database,
because the counting wrapper sits in front of every read and has no business inside a timed
number. `bench/suites/execution.bench.mjs`'s `queryCounters` is the pattern to copy: build
the fixture into the counting database, `resetCounters()` at the boundary between fixture
and subject, snapshot, then `readCounters()` **before** `close()`.

## Scan, seek, and batched row resolution

- sequential iterate throughput over a populated table,
- a point read on the primary key,
- a multi-key seek (`where pk in (...)`) at a few key counts,
- **the batched row resolution behind a secondary index.** `StoreTableScan.resolveRowBatch`
  pages at `ROW_RESOLUTION_BATCH` (256). Choose match-set sizes that straddle that boundary:
  well under 256, exactly 256, and a couple of multiples of it. The interesting failure is a
  batch that stops batching — the round-trip count would jump from `ceil(n / 256)` to `n`
  while the wall-clock on an in-memory provider barely moves. The batched-read call count is
  what catches it, and it is the reason this suite exists.

State the expected round-trip count in a comment beside each size, **derived from the
imported constant rather than from the literal 256**, so a change to the constant moves the
expectation instead of turning the comments into lies. A benchmark whose expected count is
written down is one whose regression is readable without a baseline.

`packages/quereus-store/test/index-scan-batching.spec.ts` already asserts this shape at the
spec level (`ceil(300 / 256) == 2` batches, and so on) — it is the reference for how the
counts come out, and the benchmark should agree with it.

## Transaction commit

`TransactionCoordinator` buffers every mutation of a transaction and writes them out on
commit — in one atomic batch when the provider has a shared commit domain, else one batch
per store. Queue N operations and commit, across a range of N (say 1, 10, 100, 1000,
10000). The shape of round-trips-versus-N is the measurement: flat per commit is correct,
linear in N means a per-operation cost that should have been per-batch.

**This is the benchmark that needs the write counters the harness ticket adds.** The read
counters cannot answer it: any workload queueing N operations must touch N rows, so reads are
linear in N no matter what the commit path does. The batch-round-trip count is the number
that must stay flat.

Note that `createInMemoryProvider` (what the timed and counting databases are built over)
implements no `beginAtomicBatch`, so the store commits **one batch per store**, not one per
transaction. The expected flat count is therefore per store touched, not literally one —
write the expectation down accordingly, and say in a comment that a provider with a shared
commit domain would show a different (also flat) number.

The workload must be repeatable back-to-back without growing the store. An `update` over a
fixed key range against a shared fixture is the shape that fits — the same fixed-point
argument `mutation/update-where-1k` already documents. **Verify before relying on it that
the store actually writes a row whose new value equals its old one**; if the update path
short-circuits a no-op write, the second and every later call queues nothing and the
benchmark measures an empty commit. If it does short-circuit, alternate the written value
between two constants instead: the table stays the same size and each call costs the same,
which is what repeatability actually requires.

## Index build

Building a secondary index over an existing table (`create index` on a populated table)
streams through `buildIndexEntries`. One benchmark at one table size, reported in round
trips. A repeatable `fn` creates and drops the index each call.

**The drop must actually erase**, or the second call builds an index over a store that
already holds the first call's entries and the benchmark measures something else entirely.
Two facts about the two providers, which differ here and which the benchmark has to be
written around:

- `createInMemoryProvider` — used by the timed database — **does** implement
  `deleteIndexStore`, and it erases: it clears a live store so an existing handle observes
  the erase, then drops the map entry.
- `createCountingProvider` — used by the `counters()` pass — implements **neither**
  `deleteIndexStore` nor `deleteTableStores`, deliberately and with a NOTE saying so. A
  drop-then-recreate against it reads the dropped store's entries back. The counters pass
  runs exactly once, so it must create the index **once and not drop it**.

**Where to put the assertion.** "Assert the index store is empty after the drop" does not
work as written: `erase` removes the map entry, so asking the provider for that index store
afterwards lazily creates a fresh empty one and the check passes no matter what. Assert
instead at the **start of each `fn` call** that the index store is empty or absent before the
`create index` — that is the check that actually fires when a drop stops erasing. Reaching the
provider needs the handle change the harness ticket makes to `openStoreDatabase()`.

## Catalog rehydration

Reopening a database with a non-trivial schema: build the schema over a store-backed
`Database`, close it, then open a fresh `Database` over the **same provider instance** and
measure the cost of rebuilding the schema from the `__catalog__` store. Reported in round
trips against that store, which is the count that would catch a rehydration that reads
per-table instead of scanning once.

Reusing the same `Database` measures nothing; building a new provider measures an empty
catalog. Assert the rehydrated schema has the expected table count, or the benchmark can
pass while reading nothing.

The schema should be non-trivial the way a real one is — tens of tables, some with
secondary indexes, some views — rather than one wide table.
`bench/apply-schema-unchanged.mjs` already contains a 54-table / 14-view declaration
generator (`buildDeclaration`); reuse its shape rather than inventing a second one.

# Edge cases & interactions

- **Use only public exports of `@quereus/store`,** reached through the accessor in
  `bench/lib/store-counters.mjs` — never a second import site, which would let an unbuilt
  store package kill the whole `yarn bench` run at suite enumeration.
- **Batching hides per-call setup.** Calibration times several consecutive `fn` calls as one
  sample for sub-millisecond work, so an `fn` that allocates a large fixture per call
  measures the allocation. Build fixtures in `setup`.
- **The commit benchmark at N = 10000 is not repeatable back-to-back for free.** Either
  reset inside `fn` and accept that the reset is in the number, stated in a comment, or pin
  it out of calibration with the reason written down. Do not leave a benchmark whose cost
  climbs across its own samples.
- **Wall-clock budget.** This section is on the order of a dozen benchmarks on top of a run
  the docs currently put at ~100 s for 46 benchmarks (the upstream ticket's "~35 s" is
  stale). Measure and report the added time. If it is large, the lever is
  `CALIBRATION.targetTotalMs` in `bench/lib/calibrate.mjs`, which shortens every benchmark
  evenly — not silently shrinking these workloads. If the honest number is too large to
  accept, say so and propose which sizes to drop rather than trimming them quietly.
- **Explicit `begin;` / `commit;` in SQL works** — see
  `packages/quereus/test/logic/10.1.2-ddl-in-transaction.sqllogic`.

# Tests

- Every benchmark asserts its own result, as `docs/benchmarking.md` requires: a row count, a
  round-trip count, an entry count before the index build.
- Two consecutive runs produce byte-identical counter blocks for this suite. That is the
  acceptance criterion; identical timings is not.
- The stated expected round-trip counts hold: `ceil(n / ROW_RESOLUTION_BATCH)` batched reads
  for a match set of n, and a commit batch-round-trip count that does not grow with the
  queued operation count.

# TODO

- Sequential iterate, point read, and multi-key seek benchmarks, reporting round trips
- Batched row-resolution benchmarks at match-set sizes straddling `ROW_RESOLUTION_BATCH`, with expected round-trip counts derived from the imported constant
- Transaction-commit benchmarks across a range of queued operation counts, reporting the batch-round-trip shape; first verify whether a same-value update short-circuits, and pick the workload accordingly
- Index-build benchmark, asserting the index store is empty before each build, with a `counters()` pass that creates the index once and never drops it
- Catalog-rehydration benchmark over a many-table schema, reusing the provider and asserting the rehydrated table count
- Measure and report the wall-clock this suite adds to `yarn bench`
- Extend the suite's section in `docs/benchmarking.md` with what these benchmarks cover and what their counter blocks mean
