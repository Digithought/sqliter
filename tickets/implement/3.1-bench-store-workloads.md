---
description: Run the existing query and write benchmarks a second time against the persistent-storage code path instead of the in-memory table, and report how many times each one went to storage.
prereq: bench-backend-dimension
files:
  - packages/quereus-store/src/testing/memory-provider.ts  # NEW - the shared plain in-memory provider
  - packages/quereus-store/src/testing/index.ts            # exports it
  - packages/quereus-store/src/testing/kv-counting-store.ts # createCountingProvider - the counters-pass provider
  - packages/quereus-store/src/common/isolated-store.ts    # createIsolatedStoreModule - how a store db is built
  - packages/quereus/test/logic.spec.ts                    # lines ~458-580, the existing store-mode setup to copy
  - packages/quereus/bench/lib/backends.mjs                # gains STORE_MEM_BACKEND
  - packages/quereus/bench/lib/store-counters.mjs          # NEW - round-trip snapshot helper
  - packages/quereus/bench/suites/execution.bench.mjs      # backend set grows by one entry
  - packages/quereus/bench/suites/mutation.bench.mjs       # same
  - docs/benchmarking.md
difficulty: medium
---

# Why

`yarn test:store` exists because the persistent path is different enough to need its own
full correctness run - key encoding, batched row resolution, transaction commit, index
build and catalog rehydration are all code the memory table never executes. None of it has
any performance coverage. A change that doubled the storage round trips behind a
secondary-index scan would leave `yarn bench` unchanged.

`bench-backend-dimension` built the mechanism. This ticket supplies the backend and the
metric that actually matters for it.

# Architecture

## No new dependency is needed

`packages/quereus/package.json` already lists `@quereus/store` and
`@quereus/plugin-leveldb` as workspace devDependencies (they are what `yarn test:store`
runs on), and `node_modules/@quereus/*` are symlinks into `packages/`. Node resolves
symlinks to their real path, so `@quereus/store`'s import of `@quereus/quereus` and the
bench's own `../../dist/src/index.js` land on the same file and therefore the same module
instance. There is no dual-instance hazard and nothing to add to a manifest.

Everything here needs a built `dist/` in **both** packages, which `yarn build` already
produces in dependency order. A bench run against a stale `packages/quereus-store/dist`
measures the wrong code; say so in the docs next to the existing `dist/` warning.

## The store backend

One new descriptor in `bench/lib/backends.mjs`:

- `id: 'store-mem'`, `label: 'StoreModule (isolation-wrapped) over an in-memory key-value provider'`
- `open()` builds a provider, calls `createIsolatedStoreModule({ provider })`, registers it
  as `'store'` and sets `default_vtab_module` to `'store'`; `close()` closes the `Database`
  and then the module's `closeAll()`.

Two decisions, both settled here rather than left to the implementer:

**It is isolation-WRAPPED by default.** `createIsolatedStoreModule` wraps `StoreModule`
with the isolation layer for read-your-own-writes, rollback and savepoints. That is what
`yarn test:store` runs and what a real deployment runs, so it is what the default row must
measure. A bare-`StoreModule` reading is a different question ("what does the wrapper
cost"), and answering it is `feat-bench-isolation-and-sync-suites`, not this ticket. Do not
add a second backend id for it here.

**The provider is in-memory, not on disk.** That is the point: it isolates *store-layer*
cost from *disk* cost, it is deterministic, and it is cheap enough to run on every `yarn
bench`. Disk is `bench-store-leveldb`, opt-in and out of any gate.

## The in-memory provider is shared, not local

`@quereus/store` exports the `InMemoryKVStore` class but no in-memory *provider*. The
package's own tests each paste their own copy - over twenty of them, which is the subject
of `debt-store-test-shared-inmemory-provider` (backlog). Writing a twenty-first copy inside
`bench/` is the wrong move.

Add `createInMemoryProvider(options?)` to `@quereus/store/testing`, next to the
`createCountingProvider` that the same backlog ticket's review already collapsed. It must
match that sibling's settled conventions, because they are the same conventions and a
divergence here re-opens a question that has been answered:

- Store names come from `buildDataStoreName` / `buildIndexStoreName` and the reserved
  `CATALOG_STORE_NAME` / `STATS_STORE_NAME` constants - never hand-composed template
  strings, which drop the builders' lowercasing and reach a store no real backend would
  have used.
- `getStatsStore` returns ONE unified `__stats__` store for every table, as the
  `KVStoreProvider` interface specifies (its schema/table arguments are documented as
  ignored). The per-table spelling most local copies use is the accidental variant.
- `options.costProfile` is spread in **only when present**, so a provider that declares
  nothing has no such property at all. Two existing specs depend on exactly that shape.
- Unlike `createCountingProvider`, it **implements `deleteIndexStore` and
  `deleteTableStores`, and they erase** rather than dropping the handle. A benchmark that
  drops and recreates a table under the same name must not read the dropped store's
  entries back, and the index-build benchmark in the next ticket does exactly that.

This ticket does **not** sweep the twenty existing copies onto the new factory - that stays
`debt-store-test-shared-inmemory-provider`'s job, and doing it here would make this a
merge-conflict magnet across the whole store test suite. Append an arm to that ticket
saying the factory now exists.

## Round trips are the primary metric

Time on a store backend is a machine fact. The number that is stable across machines, and
the one a later gate keys on, is how many times the engine went to storage and with how
many keys.

The harness already has the right seam: `counters()` runs ONCE, untimed, after the timed
loop. So the timed `fn` runs against the plain provider (no counting overhead in the
number), and `counters()` builds a *second* database over
`createCountingProvider(map, 'all')` and drives the same statement. `mutation.bench.mjs`
already establishes the own-database-in-`counters()` pattern for three of its four entries.

`bench/lib/store-counters.mjs` returns a nested block, which `flattenCounters` in
`compare.mjs` already walks to arbitrary depth:

```js
{
  engine: { /* the existing WorkCounterSnapshot from snapshotStatement */ },
  store: {
    'main.bench_t':             { iterateEntries, getManyCalls, getManyKeys, singleGets },
    'main.bench_t_idx_bench_t_val': { /* same four */ },
  }
}
```

The four fields need naming care, because `CountingKVStore`'s raw counters do not mean what
their names suggest. Its `getMany` deliberately routes each key through the wrapper's own
counted `get`, so `getCount` includes every key of every batch. Therefore:

- `getManyCalls` = round trips (batched reads issued),
- `getManyKeys` = key volume carried by them,
- `singleGets` = `getCount - getManyKeyCount`, the reads that were genuinely one-at-a-time,
- `iterateEntries` = entries pulled from `iterate()`.

Derive `singleGets` in `store-counters.mjs`, once, with the reason in a comment - not at
each call site. A negative value is impossible and means the counting contract changed:
throw rather than report it.

Store names in the block are the BUILT names the counting map is keyed by, so they are
stable across runs and diffable path-by-path. Do not re-key them by table name.

# Edge cases & interactions

- **Not every workload runs on the store.** Anything asserting memory-module behaviour, and
  anything the store's cost model plans differently, skips with a stated reason via the
  `skip()` entry point from `bench-backend-dimension` - never by omission. `logic.spec.ts`'s
  `MEMORY_ONLY_FILES` set and the `-- requires-capability:` mechanism are the precedent for
  *what* diverges; read them before deciding, and put the reason in the skip string.
- **The store path is slower, sometimes much slower.** Calibration adapts (`warmupTargetMs`
  250, `targetTotalMs` 1000, `maxTotalMs` 5000, `minSamples` 5), but `minSamples` is checked
  *before* `maxTotalMs`, so a 3-second call still costs ~5 samples plus warmup. Measure the
  added wall-clock of the store rows and report it; if any single benchmark approaches
  `BENCH_TIMEOUT_MS` (120 s, and it covers `setup` too), pin it with `iterations`/`warmup`
  and a comment saying why, rather than raising the timeout.
- **A pinned benchmark forfeits its spread figure.** Five samples cannot support a quartile
  range. Prefer a smaller row count for a store row over pinning it.
- **Populate cost is setup, not measurement** for the read workloads - but for the mutation
  workloads it *is* the measurement, and inserting 10k rows through the isolation overlay
  plus a transaction commit is a different order of cost than the memory table. Check
  `mutation/bulk-insert-10k@store-mem` explicitly before assuming 10k rows is the right size
  there; a smaller store-side row count with the count in the name is better than a
  benchmark nobody will wait for.
- **The counters pass builds a second full database.** Three mutation entries already do,
  costing ~340 ms of a ~48 s run; the store versions cost more. The existing NOTE in
  `mutation.bench.mjs` names the fix (populate fewer rows in `counters()` than in `fn` -
  counts need only be *the same every run*, not the timed row count). Take that option
  rather than growing the untimed pass.
- **Counter portability is an assumption, not a fact.** `bench-regression-gate` records that
  nothing has yet compared counter blocks across two machines, and that
  `bug-join-order-depends-on-wall-clock` is a live mechanism by which plan choice - and so
  counts - could differ on a slower machine. Store round trips are reported here as facts
  about this run; do not add a gate.
- **Every store backend must leave no state behind.** `close()` must close the module, not
  only the `Database`. A leaked handle trips the worker's leaked-handle exit path, which
  presents as a hang.

# Tests

- `packages/quereus-store/test/`: the new `createInMemoryProvider` runs the existing
  `runStoreNameDistinctness` and `runStoreReclaimConformance` batteries (the reclaim one is
  what pins the erase semantics), plus a case that `costProfile` is absent when unset.
- Bench-side: one run of `yarn bench --filter @store-mem` completes, every row either
  produces numbers or states a skip reason, and a second run produces byte-identical
  `store` counter blocks. Identical counters across two runs is the acceptance criterion for
  the metric; identical *timings* is not and must not be asserted.
- `yarn workspace @quereus/store test` and `yarn workspace @quereus/store typecheck` clean.

# TODO

- Add `createInMemoryProvider(options?)` to `packages/quereus-store/src/testing/`, matching `createCountingProvider`'s naming/stats conventions, with erasing delete hooks and an only-when-present `costProfile`
- Export it from `@quereus/store/testing` and document it in `packages/quereus-store/README.md` alongside the counting doubles
- Run the name-distinctness and reclaim conformance batteries against it
- Add `STORE_MEM_BACKEND` to `bench/lib/backends.mjs`: isolation-wrapped `StoreModule` over the new provider, with a `close()` that also calls the module's `closeAll()`
- Add it to the execution and mutation suites' backend sets
- Add `bench/lib/store-counters.mjs`: build a counting-provider database, drive the statement, return the nested `{engine, store}` block with `singleGets` derived once
- Give every store-backed benchmark a `counters()` that returns that block
- Skip visibly, with a stated reason, any workload the store cannot meaningfully run
- Measure and report the wall-clock the store rows add to `yarn bench`; size or pin anything that approaches the per-benchmark timeout
- Document the store backend, the round-trip metric and what each of its four counts means in `docs/benchmarking.md`
- Append an arm to `tickets/backlog/debt-store-test-shared-inmemory-provider.md` recording that the shared factory now exists
