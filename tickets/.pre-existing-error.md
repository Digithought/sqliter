## Pre-existing failure: `yarn docs:check` — docs/runtime.md size ratchet

Command run (from repo root): `yarn docs:check` (equivalently `node scripts/check-docs.mjs`)

Failing check output:
```
docs/runtime.md: 13983 words exceeds its ratchet of 13270 (+713) — a doc may shrink, never grow

1 documentation failure(s). See docs/doc-conventions.md.
```

Why this is pre-existing / not caused by this ticket's work: this ticket (`debt-package-readme-stability-banners`) only edited package `README.md` files under `packages/*/README.md` (adding stability banners near the top of each). It never touched `docs/runtime.md` or any file under `docs/`. `git status` at the end of this ticket's work shows only the twelve `packages/*/README.md` files as modified — `docs/runtime.md` is not among them, so its word count could not have moved from this ticket's edits. The word-count ratchet (Check B in `scripts/check-docs.mjs`) is comparing `docs/runtime.md` against a budget recorded before this ticket started.
