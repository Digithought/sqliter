# Pre-existing failure: `yarn docs:check` — docs/runtime.md over the word cap

## Command

From the repo root:

```
yarn docs:check          # → node scripts/check-docs.mjs
```

Also reachable as the first step of `yarn check`.

## Failure

```
docs/runtime.md: 12103 words exceeds the 12000-word cap for an unratcheted doc — split it, or record it with --update-ratchet --force and say why in the commit message

1 documentation failure(s). See docs/doc-conventions.md.
```

Exit code 1. The failing check is "Check C — size ratchet" in `scripts/check-docs.mjs`
(the `over-cap` verdict, `scripts/check-docs.mjs:568`). `docs/runtime.md` has no entry in
`docs/.doc-budget.json`, so it is judged against the flat 12000-word cap, which has no
grace band.

`yarn lint` and `yarn test` both pass; this is the only failing check in the tree.

## Why this is pre-existing

- It fails at `8632f26ab` (the `bench-counter-tests-and-docs` implement commit) with a
  clean working tree, before any edit made by the review pass — I ran `yarn docs:check`
  first thing and got this identical message, byte for byte.
- `docs/runtime.md` was last modified at `95c116c3a`
  (`ticket(review): bench-counter-suite-passes`), the commit *before* the one under
  review. The `bench-counter-tests-and-docs` diff touches `docs/benchmarking.md` and
  `packages/quereus/test/bench-comparison.spec.ts` only — it never opens `runtime.md`.
- The review pass's own doc edits (`docs/benchmarking.md`, `docs/architecture.md`) leave
  the output unchanged: still exactly this one failure, no new one and no near-cap notice
  on either edited file.

## Note for triage

103 words over the cap. `scripts/check-docs.mjs` itself offers two remedies and they are
not equivalent — splitting `runtime.md`, or recording it in `docs/.doc-budget.json` with
`--update-ratchet --force` plus a justification in the commit message. Which one is right
is a judgement about whether `runtime.md` has outgrown one file, so it wants a deliberate
decision rather than the smallest edit that turns the check green. Trimming 103 words of
prose out of it would clear the gate while leaving that question unasked.
