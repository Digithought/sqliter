# Pre-existing failure: `yarn docs:check` fails at HEAD (invariants.md OPT-022)

- Command: `yarn docs:check` (repo root; `node scripts/check-docs.mjs`)
- Failing output (verbatim, present on a clean tree at HEAD `558f0cb9`, before
  any change from ticket `feat-uncorrelated-in-semijoin`):

  ```
  docs/invariants.md:201: invariant 'OPT-022' has 2 'guard:' lines — expected exactly one
  docs/invariants.md:201: invariant 'OPT-022' body is 149 words (max 120) — it is two invariants, or it is rationale wearing an invariant's clothes
  ```

- Why pre-existing: `docs/invariants.md` is untouched by this ticket (it edits
  only `docs/optimizer-rules.md`, `docs/runtime.md`, `docs/runtime-caching.md`);
  the same two lines fail on the unmodified checkout.

Also present at HEAD (root cause already ticketed, so listed here only for
completeness — do not re-triage): `docs/optimizer-rules.md` exceeds its
12000-word cap (12085 words at HEAD). The split is specified in
`backlog/debt-split-optimizer-rules-doc`. This ticket's required rule-catalog
entry raises the count to 12174 (net +89 after deleting the retired
`ruleInSubqueryCache` bullet); promoting the split ticket clears it.
