# Pre-existing failure: `yarn docs:check`

## Command

```
yarn docs:check          # repo root — runs scripts/check-docs.mjs
```

## Failures

```
docs/invariants.md:989: invariant 'SCH-001' body is 147 words (max 120) — it is two invariants, or it is rationale wearing an invariant's clothes
docs/schema.md: 15841 words exceeds its ratchet of 15678 (+163) — a doc may shrink, never grow
docs/sql-ddl.md: 12501 words exceeds the 12000-word cap for an unratcheted doc — split it, or record it with --update-ratchet --force and say why in the commit message

3 documentation failure(s). See docs/doc-conventions.md.
```

## Why this is pre-existing

Reproduced identically in a clean `git worktree` checked out at `bfa86e42`
(`ticket(implement): builtin-scalar-function-declared-return-types`), i.e. before any
edit from the review pass. All three files were last touched by `7d6bba68`
(`ticket(fix): bug-hidden-implicit-index-leaks-into-introspection`), several commits back.

The review pass for `builtin-scalar-function-declared-return-types` edited only
`docs/functions.md`, `docs/sql-functions.md` and `docs/types.md` — none of the three
failing files, and none of the three edited files is flagged by the check.

`yarn docs:check` is the first step of `yarn check`, so this blocks the full gate.

Note: `yarn lint`, `yarn test` (all workspaces) and `yarn workspace @quereus/quereus run
test:plans` are all green at the same tree.
