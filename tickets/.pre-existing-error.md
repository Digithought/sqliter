# Pre-existing failure surfaced during review of bug-alter-add-column-relation-default-fails-to-emit

## Command

```
yarn workspace @quereus/quereus run test:store
```

(from repo root; package `@quereus/quereus`. `yarn test` — memory module — is fully green:
8672 passing, 13 pending, 0 failing.)

## Failing test

- Suite: `SQL Logic Tests (Store Mode)`
- File: `packages/quereus/test/logic/07.3.2-grouped-select-list-shape.sqllogic`, line 178
- Result: `2762 passing, 2 pending, 1 failing`

```
Error: [07.3.2-grouped-select-list-shape.sqllogic:178] row 0 mismatch.
Actual:   {"a":"x","b":"1","c":1}
Expected: {"a":"x","b":"1","a:1":"x","c":"p","c:1":1}
Block:
select jl.a, jl.b, jr.a, jr.c, count(*) c from jl join jr on jl.a = jr.a
  group by jl.a, jl.b, jr.a, jr.c order by jl.a;
```

Under the store module the grouped select list loses the duplicate-name-disambiguated
output columns (`a:1`, `c:1`) and the `jr.c` value, collapsing five result columns to
three. The memory module produces the expected five.

## Why this is pre-existing

The reviewed ticket's diff is confined to `AlterTableNode`'s `getChildren`/`withChildren`,
a comment in `runtime/emit/alter-table.ts`, tests, and docs. This review's own changes to
`packages/quereus/src/` are **comment-only** (verified: `git diff -U0 -- packages/quereus/src/`
shows no non-comment changed lines). Nothing in either touches grouped select-list shaping,
join output naming, or the store module. The failing statement contains no `ALTER TABLE`.
