# Pre-existing failure: `yarn docs:check` — docs/optimizer.md over the unratcheted word cap

## Command

From the repo root:

```
yarn docs:check     # → node scripts/check-docs.mjs
```

## Failure

```
docs/lens.md: 18310 words, 376 over its ratchet of 17934 — inside the 500-word grace band (124 left)
docs/schema.md: 12568 words, 459 over its ratchet of 12109 — inside the 500-word grace band (41 left)
docs/sync.md: 12670 words, 132 over its ratchet of 12538 — inside the 500-word grace band (368 left)
docs/optimizer.md: 12211 words exceeds the 12000-word cap for an unratcheted doc — split it, or record it with --update-ratchet --force and say why in the commit message

1 documentation failure(s). See docs/doc-conventions.md.
```

Only the `docs/optimizer.md` line is a failure; the three above it are warnings inside
the grace band.

## Why it is pre-existing

`docs/optimizer.md` is not touched by the ticket that found this
(`bug-table-rename-breaks-dependent-assertions`). It is byte-identical at that ticket's
parent commit `0c19d961` and at its own commit `d35d0b7e` — 12211 words in both. It
last changed under ticket `bug-filter-row-estimate-lost-in-materialization-pass`
(commit `ab5670d8`), which is where it crossed the 12000-word cap for a doc with no
recorded ratchet.

## Note on `tickets/.pre-existing-known.md`

That file maps "`yarn docs:check` (word-count ratchet: docs/module-authoring.md,
docs/schema.md, docs/sync.md)" to `debt-doc-size-ratchet-red-at-head`. That mapping does
not cover this failure: the ticket is in `tickets/complete/`, the doc named here is
`optimizer.md` (not in that list), and the rule tripped is the hard 12000-word cap for an
**unratcheted** doc, not the ratchet grace band. The three grace-band warnings above are
the tail of the tracked set and are not being re-reported.

## Scope note

`docs/schema.md` has only 41 words of grace left. Any ticket that adds a paragraph there
will turn a warning into a failure, so a fix that only addresses `optimizer.md` will be
short-lived.
