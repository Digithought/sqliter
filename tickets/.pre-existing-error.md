# Unrelated failure seen during review of `analyze-unresolvable-name-is-silent`

## Command

From the repo root:

```
yarn test
```

(21m 12s wall clock; the failure is in the `@quereus/quereus` workspace's mocha run.)

## Failure

```
  1) Aggregate algebra declarations
       negative twin — the harness catches a broken declaration
         the single-slot "mirror binary +" alternative fails merge-associativity on the mixed domain:
     Error: Timeout of 10000ms exceeded. For async tests and hooks, ensure "done()" is called;
     if returning a Promise, ensure it resolves.
     (C:\projects\quereus\packages\quereus\test\incremental\aggregate-algebra.spec.ts)
```

Path: `packages/quereus/test/incremental/aggregate-algebra.spec.ts`
Totals for that run: 1869 passing, 1 failing.

## Why it looks unrelated to this ticket

- The ticket's diff touches only the `ANALYZE` statement path (`planner/building/analyze.ts`,
  `runtime/emit/analyze.ts`, the parser's `analyzeStatement`, plus `ANALYZE` tests and docs).
  This spec exercises aggregate algebra law-checking and never runs `ANALYZE`.
- Re-run in isolation from the repo root it passes in **421ms**, all 22 tests green:
  `node --import ./packages/quereus/register.mjs node_modules/mocha/bin/mocha.js "packages/quereus/test/incremental/aggregate-algebra.spec.ts"`.
  The failure is a 10s mocha timeout hit only inside the full suite, i.e. load/timing
  dependent rather than a deterministic assertion failure.
- The immediately preceding implement run of the same ticket reported the full suite green,
  so the test is not newly broken by these edits.

Nothing was skipped, disabled, or loosened.
