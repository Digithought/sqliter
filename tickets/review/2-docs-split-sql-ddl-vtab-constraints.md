---
description: The SQL reference page about creating tables was about to blow past the size limit the docs checker enforces, so its virtual-table and constraint chapters were moved into two new sibling pages and every link pointing at them was updated.
files:
  - docs/sql-ddl.md           # shrank 11,750 → 7,531 words
  - docs/sql-vtab.md          # NEW — § 6 Virtual Tables, 1,988 words
  - docs/sql-constraints.md   # NEW — § 7 Constraints and Indexes, 2,282 words
  - docs/sql.md               # topic table (row edit + 2 new rows), 2 "Moved to" stubs
  - docs/functions.md, docs/invariants.md, docs/runtime.md, docs/schema.md, docs/sql-alter.md   # inbound anchors
  - docs/.stability.json      # 2 new Stable entries
  - packages/quereus/src/schema/manager.ts, packages/quereus/src/schema/schema-differ.ts        # comment-only § 6.3 markers
difficulty: easy
---

Pure documentation move plus two comment-only TypeScript edits. No runtime behaviour touched.

## What landed

`docs/sql-ddl.md` was 11,750 words against a hard 12,000-word cap with no grace band, so
`node scripts/check-docs.mjs` (step one of `yarn check`) printed a near-cap notice and roughly one
added paragraph would have turned it red. Two whole top-level chapters moved out into new siblings
of the `sql.md` family:

| New document | Content | Words |
| --- | --- | --- |
| `docs/sql-vtab.md` | `## 6. Virtual Tables` (6.1–6.3) | 1,988 |
| `docs/sql-constraints.md` | `## 7. Constraints and Indexes` (7.1–7.8) | 2,282 |

`docs/sql-ddl.md` is now 7,531 words — a ~4,470-word margin. No ratchet entry was added; both new
documents are well under the cap.

Section numbering and every heading were kept **verbatim** — `## 6. Virtual Tables` is still an H2,
`### 6.3 Indexes on Virtual Tables` is unchanged, § 7.1–7.8 unchanged. Only the *file* half of each
inbound link changed. Both new documents carry the `docs/sql-alter.md` opening exactly: H1,
`> **Stability: Stable**` banner, then the `Part of the [Quereus SQL Reference](sql.md) …` line.

No stub was left inside `sql-ddl.md` — `sql.md` already holds the "Moved to …" stubs for § 6 and
§ 7, and both were repointed at the new files.

## Verification performed

- **Verbatim proof.** Extracted lines 654–833, 835–1176 and 1–651 from `git show HEAD:docs/sql-ddl.md`
  (baseline `c2b973e0`) and `diff`ed each against the corresponding new/remaining body. All three
  diffs empty. Anyone re-checking can rerun that: the body of each new document is everything from
  line 7 onward (`tail -n +7`).
- **`node scripts/check-docs.mjs`** — exits 0, ends `Docs OK`, and no longer prints any line for
  `docs/sql-ddl.md`. It prints no line for either new document. Two notices remain and are
  pre-existing: `docs/design-isolation-layer.md` (127 from the cap — that is sibling ticket
  `3-docs-split-isolation-design`'s job) and `docs/lens.md` (471 over its ratchet, inside the
  grace band). Both printed identically at baseline.
- **`git diff docs/.doc-budget.json`** — empty, as required.
- **Heading slugs.** Listed every heading in all three documents; no duplicate heading text within
  any one file, so no `-1`/`-2` slug suffix was introduced and no anchor silently shifted.
- **`yarn lint`** — passes (53s).
- **`yarn workspace @quereus/quereus run test:single packages/quereus/test/documentation.spec.ts`**
  — 10 passing, including "should have all relative doc links resolve to existing files".
- **`yarn test` was deliberately not run.** The diff is markdown plus two comments; no engine code
  path changed. If the reviewer wants belt-and-braces, `yarn test` is the command, but nothing in
  this diff can move it.

## Links repointed (all 12, regenerated by grep, not by trusting line numbers)

`grep -rn --include=*.md --include=*.ts -E 'sql-ddl\.md#(6|7)' . --exclude-dir=node_modules
--exclude-dir=dist --exclude-dir=.git --exclude-dir=tickets` now returns nothing.

- → `sql-vtab.md#63-indexes-on-virtual-tables`: `docs/functions.md`, `docs/invariants.md` (×2),
  `docs/schema.md`, `docs/sql-alter.md` (×4 across lines 51/71/131/246)
- → `sql-vtab.md#6-virtual-tables`: `docs/sql.md` stub
- → `sql-constraints.md#7-constraints-and-indexes`: `docs/sql-alter.md`, `docs/sql.md` stub
- → `sql-constraints.md#76-foreign-key-constraint`: `docs/runtime.md`

**Link *text* was updated too, not only the target.** Four sites named the old document inside the
link label (`[sql-ddl.md §6.3](…)`, `[SQL DDL § 6.3 …](…)`, `[DDL § 7.6 FOREIGN KEY](…)`); leaving
those would have shipped a label that lies about where the link goes. They now read
`[sql-vtab.md §6.3]`, `[SQL Virtual Tables § 6.3 …]`, `[Constraints § 7.6 FOREIGN KEY]`. **This is
the main judgement call in the diff and the thing most worth a reviewer's eye** — the ticket only
asked for the file half. `docs/sql-alter.md:132`'s label `[§2.6 → §7]` was left alone on purpose:
it names section numbers, not files, and both numbers are still accurate.

Three prose markers the checker structurally cannot see (`bareDocRefs()` in `scripts/check-docs.mjs`
stops at `.md`, so a `§ 6.3` marker keeps passing after § 6.3 moves — that hole is the open ticket
`backlog/debt-check-docs-validate-section-markers`, not re-filed here) were fixed by hand:
`manager.ts:2500`, `manager.ts:2605`, `schema-differ.ts:329`, all now naming `docs/sql-vtab.md §6.3`.
Re-grep (`grep -rn 'sql-ddl.md §' packages/quereus/src docs`) leaves only markers pointing at
sections that *stayed* in `sql-ddl.md` (§ 2.6.1, § 2.6 CHECK/Generated Columns, § Order Independence,
§ Declaration Syntax) — those are correct as-is.

## `docs/sql.md` edits

- The `sql-ddl.md` topic-table row lost its "virtual tables, constraints and indexes" clauses.
- Two new rows, `[Virtual tables](sql-vtab.md)` and `[Constraints & indexes](sql-constraints.md)`,
  placed after the ALTER row and before Views — a judgement call the ticket left open ("place them
  where the table reads naturally"); grouping the four schema documents together reads best.
- The `## 6.` and `## 7.` stubs repointed. The `<!-- NOTE: … -->` block above the table is untouched.

## Known gaps / what a reviewer should poke at

- **Row placement and the two new one-line row descriptions in `sql.md` are newly written prose** —
  the only text in this diff that is not a verbatim move. Worth reading for accuracy: does
  "Constraints & indexes | `PRIMARY KEY`, `NOT NULL`, `UNIQUE`, `CHECK`, `FOREIGN KEY`, `DEFAULT`,
  and `CREATE` / `DROP INDEX`" actually match § 7.1–7.8? (It was written off the heading list.)
- **The two new documents have no introductory paragraph** beyond the family line — `sql-alter.md`
  has a two-sentence orientation blurb, these do not, because adding one would not be a verbatim
  move. If the reviewer wants parity with `sql-alter.md`, that is a small additive edit; there is
  ~9,900 words of headroom in each new file.
- **Section numbers now span three documents** (§ 2.x in `sql-ddl.md`, § 6 in `sql-vtab.md`, § 7 in
  `sql-constraints.md`). Already true of the family before this change (§ 2.7 lives in `sql-alter.md`,
  § 8/§ 9 in `sql-txn.md`), and `sql.md` is the map, but it is the structural consequence to sanity-check.
- **`docs/.stability.json` is shared with sibling tickets `1-docs-split-optimizer-costing` (landed)
  and `3-docs-split-isolation-design` (not yet).** It was re-read immediately before writing and both
  new entries went in at their existing sort positions (`sql-constraints.md` after `sql-alter.md`,
  `sql-vtab.md` after `sql-views.md`). If ticket 3 lands concurrently, this file is the merge point.
- No test asserts that a moved section's anchors still resolve *from the new file* — `check-docs.mjs`
  does that, and it passes, but there is no unit test pinning it.

## Not done, on purpose

- `yarn test` (see above).
- No new invariant minted; the three existing `doc:` links into the moved sections were repointed,
  which the ticket names as the whole obligation.
- `INVARIANT_HEADING` in `scripts/check-docs.mjs` untouched (explicitly out of scope).
- `docs/.doc-budget.json` untouched.
- No README changes — neither `README.md` nor `packages/quereus/README.md` references any `sql-*.md`
  document, so there was nothing to repoint.
