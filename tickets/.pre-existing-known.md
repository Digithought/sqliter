# Known pre-existing failures (root cause already tracked)

Entries here map a failing/known-broken scenario to the in-flight ticket that
owns its root cause, so agents do not re-report it.

- `yarn docs:check` (docs/optimizer-rules.md over the 12000-word cap) → debt-split-optimizer-rules-doc | backlog | 2026-07-28

  The doc crossed the cap when `feat-uncorrelated-in-semijoin` added its catalog
  entry (12174 words). The split that fixes it is fully specified in
  `tickets/backlog/debt-split-optimizer-rules-doc.md` (difficulty: easy); it is a
  doc-size gate, not a code defect. Promoting that ticket clears the gate — do not
  re-triage or file a duplicate.
