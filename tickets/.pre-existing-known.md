# Known pre-existing failures (root cause already tracked)

Entries here map a failing/known-broken scenario to the in-flight ticket that
owns its root cause, so agents do not re-report it.

- `yarn docs:check` (word-count ratchet: docs/schema.md, docs/sync.md) → debt-docs-size-ratchet-red-again | in-flight | 2026-08-03
  (was debt-doc-size-ratchet-red-at-head, now completed; both docs drifted back over budget)
