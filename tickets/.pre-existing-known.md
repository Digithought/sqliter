# Known pre-existing failures (root cause already tracked)

Entries here map a failing/known-broken scenario to the in-flight ticket that
owns its root cause, so agents do not re-report it.

- `yarn docs:check` (word-count ratchet: docs/sync.md) → docs-split-sync-protocol | in-flight | 2026-08-04
  (docs/schema.md's half of this was fixed by docs-split-schema-rename-detection; docs/sync.md is still over its ratchet)
